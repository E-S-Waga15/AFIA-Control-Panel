# Environment Configuration for MedLife Control Panel

# Base path for deployment (only needed if deploying to a subdirectory)
# For root domain deployment, leave empty or set to '/'
# For subdirectory deployment (e.g., domain.com/medlife/), set to '/medlife'
VITE_BASENAME=

# API Base URL (if using a separate API server)
VITE_API_BASE_URL=

# Other environment variables can be added here as needed
